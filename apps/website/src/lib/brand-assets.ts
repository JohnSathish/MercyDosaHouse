import receiptLogoImage from '@/assets/logo.png';

/** Public path (manifest, OG tags, etc.) */
export const RECEIPT_LOGO_PATH = '/images/logo.png';

/** Bundled logo URL — reliable in receipts, print, and PDF */
export const RECEIPT_LOGO_BUNDLED_SRC = receiptLogoImage.src;
